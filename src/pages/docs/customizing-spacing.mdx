---
title: 自定义间距
shortTitle: 间距
description: 自定义间距和尺寸。
---

import { SpacingScale } from '@/components/SpacingScale'

通过 `theme`里的`spacing`字段定制Tailwind里的[默认尺寸和间距](#default-spacing-scale).

```js
// tailwind.config.js
module.exports = {
  theme: {
    spacing: {
      '1': '8px',
      '2': '12px',
      '3': '16px',
      '4': '24px',
      '5': '32px',
      '6': '48px',
    }
  }
}
```

默认情况下，间距会被`padding`, `margin`, `width`, `height`, `maxHeight`, `gap`, `inset`, `space`以及`translate`核心插件继承。

---

## 继承默认间距


就像[主题](/docs/theme#extending-the-default-theme)里面描述的，如果你想继承默认主题的间距，你可以使用`theme.extend.spacing`字段：

```js
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      spacing: {
        '13': '3.25rem',
        '15': '3.75rem',
        '128': '32rem',
        '144': '36rem',
      }
    }
  }
}
```

这除了生成默认的间距，还会生成`p-13`, `m-15`以及`h-128`类。

---

## 覆盖间距

就像[theme documentation](/docs/theme#overriding-the-default-theme)里面面熟的，如果你想覆盖默认间距，直接在`theme.spacing`字段上添加你的自定义间距：

```js
// tailwind.config.js
module.exports = {
  theme: {
    spacing: {
      sm: '8px',
      md: '12px',
      lg: '16px',
      xl: '24px',
    }
  }
}
```

这只会生成`p-sm`, `m-md`, `w-lg`以及 `h-xl`，而不会生成默认间距。

---

## 默认间距

默认情况下，Tailwind提供了一个全面的间距列表。这些值都是成比例的，一个单位等于`0.25rem`，在普通浏览器中默认为`4px`，所以`16`就是`16 * 0.25rem = 4rem`。

<SpacingScale />
